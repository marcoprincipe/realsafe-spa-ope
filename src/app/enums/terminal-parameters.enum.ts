/**
 * Enumeração com as constantes dos parâmetros do terminal.
 */
export enum TerminalParameters {
    ACCESS_MODE = "access.mode",
    USE_VIRTUAL_KEYBOARD = "use.virtual.keyboard",
    INACTIVITY_TIMEOUT_ENABLED = "inactivity.timeout.enabled",
    INACTIVITY_TIMEOUT_VALUE = "inactivity.timeout.value",
    INACTIVITY_TIMEOUT_INTERVAL = "inactivity.timeout.interval",
    CHECK_SENSORS_ENABLED = "check.sensors.enabled",
    CHECK_SENSORS_INTERVAL = "check.sensors.interval",
    ACCOUNTING_DATE_MAX_DAYS = "accounting.date.max.days"
}
