import { ClosingDetail } from 'src/app/model';

/**
 * Interface para armazenamento dos dados de retorno da pesquisa dos detalhes do fechamento do terminal.
 */

export interface GetClosingDetailResponse {
    closingDetail: ClosingDetail
}