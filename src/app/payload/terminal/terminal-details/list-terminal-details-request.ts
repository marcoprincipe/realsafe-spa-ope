/**
 * Interface para armazenamento dos dados da requisição
 * do recurs para efetuar a pesquisa dos detalhes de um terminal.
 */

export interface ListTerminalDetailsRequest {
    terminalId: number;
}