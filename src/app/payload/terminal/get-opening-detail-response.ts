import { OpeningDetail } from 'src/app/model';

/**
 * Interface para armazenamento dos dados de retorno da pesquisa dos detalhes da abertura do terminal.
 */

export interface GetOpeningDetailResponse {
    openingDetail: OpeningDetail
}