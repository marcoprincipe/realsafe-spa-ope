/**
 * Interface para armazenamento dos dados da requisição
 * do recurs para efetuar a pesquisa dos detalhes de uma filial/unidade.
 */

export interface ListUnitDetailsRequest {
    unitId?: number;
}