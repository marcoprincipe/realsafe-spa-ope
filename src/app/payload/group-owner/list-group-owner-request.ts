/**
 * Interface para armazenamento dos dados da requisição
 * do recurso para efetuar a pesquisa de um proprietario de grupo.
 */

export interface ListGroupOwnerRequest {
    groupOwnerId?: number;
}